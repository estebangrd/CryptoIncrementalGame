# Merge Worktree
1. Confirm current directory is inside a worktree (not main repo)
2. Run all tests
3. Commit any uncommitted changes to the worktree branch
4. cd to main repo, merge the worktree branch
5. Push to remote
6. Ask user if they want to delete the worktree
